# train and val datasets (image directory or *.txt file with image paths)
# utils/datasets.py 에서 img2label_paths 함수 수정하였음.

train: ../preprocessed_dataset/train/image
val: ../preprocessed_dataset/validation/image
# test: ../preprocessed_dataset/test/image

# docker
test: ../preprocessed_dataset/image

# number of classes
nc: 4

# class names
names: ['pet', 'ps', 'pp', 'pe']
