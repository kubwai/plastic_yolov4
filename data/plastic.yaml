# train and val datasets (image directory or *.txt file with image paths)
# utils/datasets.py 에서 img2label_paths 함수 수정하였음.

train: ../tutorial_folder/train/images
val: ../tutorial_folder/valid/images
# test: ../tutorial_folder/test/images

test: ./plastic_data/test/images
# number of classes
nc: 4

# class names
names: ['pet', 'ps', 'pp', 'pe']
